import { OffreStageAccepterPipe } from './offre-stage-accepter.pipe';

describe('OffreStageAccepterPipe', () => {
  it('create an instance', () => {
    const pipe = new OffreStageAccepterPipe();
    expect(pipe).toBeTruthy();
  });
});
